// import restApi from '../api/restApi'
export default {

  /**
   * 通过用户权限获取对应摄像头树形结构
   * 该方法可有2个参数或1个参数, 1个参数时为对象, 两个参数时第一个为store对象,
   * @param {*} context
   */
  // getTreeNodeByUser (context, commit1) {
  //   return restApi.getTreeNode(1, 1)
  // }
}
